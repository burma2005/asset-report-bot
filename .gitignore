# SAM 建置產物
.aws-sam/

# 含 API Key 的部署設定（請參考 samconfig.toml.example）
samconfig.toml

# 本地測試環境變數（含 OpenRouter key）
env.json

# 本地版輸入頁（含真實端點與 API Key）
index.local.html

# 真實管理後台頁（隨機檔名、不公開；repo 內僅放展示版 admin.html）
p-*.html

# 個人測試資料與產出的報告（含真實持倉）
test_payload.json
test_report_output.html
local_test.py
portfolio_report*.html

# Google OAuth client secret
client_secret_*.json

# 部署注入產物（含真實端點，不進 git）
dist/

# Python
__pycache__/
*.pyc
.venv/

# IDE / OS
.vscode/
Thumbs.db
